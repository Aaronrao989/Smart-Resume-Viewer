# Build tools (critical for compilation)
setuptools>=69.0.2
wheel>=0.42.0
pip>=25.0.0

# Core dependencies (stable versions)
streamlit==1.28.2
numpy==1.26.4
pandas==2.0.1  # Older version with better build compatibility
scikit-learn==1.3.0
python-dotenv==1.0.0

# PDF handling (stable versions)
fpdf2==2.7.9
pillow==10.0.1

# File handling (minimal requirements)
requests==2.31.0
gdown==4.7.1

# Machine Learning
sentence-transformers==2.2.2

# LLM
openai==1.0.0
